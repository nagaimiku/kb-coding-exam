<h1>geekkobe2</h1>
<h3>Student詳細</h3>
<div class="student">
  <p><%= @student.name %></p>
  <p><%= @student.mentor %></p>
  <p><%= @student.grade %></p>
  <p><%= @student.created_at %></p>
</div>

<%= link_to "Student一覧に戻る", students_path %>
<%= link_to "編集する", edit_student_path(@student.id) %>